import getters from '@/store/users/getters';
import mutations, { initialState as state } from '@/store/users/mutations';
import actions from '@/store/users/actions';

export default {
  getters,
  state,
  mutations,
  actions,
};
