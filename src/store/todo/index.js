import actions from '@/store/todo/actions';
import getters from '@/store/todo/getters';
import mutations, { initialState as state } from '@/store/todo/mutations';

export default {
  actions,
  getters,
  mutations,
  state,
};
