import actions from '@/store/auth/actions';
import getters from '@/store/auth/getters';
import mutations, { initialState as state } from '@/store/auth/mutations';

export default {
  actions,
  getters,
  state,
  mutations,
};
